<Window x:Class="BCSTool.BackupRestoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Load Save Backup"
        Width="620"
        Height="390"
        MinWidth="540"
        MinHeight="330"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppForegroundBrush}"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="14">

        <Grid DockPanel.Dock="Bottom"
              Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="ApplyButton"
                    Grid.Column="1"
                    Content="Apply"
                    Click="Apply_Click"
                    IsEnabled="False"
                    Margin="0,0,4,0"/>

            <Button Grid.Column="2"
                    Content="Close"
                    Click="Close_Click"
                    Margin="4,0,0,0"/>
        </Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Load Save Backup"
                       FontSize="20"
                       FontWeight="SemiBold"/>

            <TextBlock Grid.Row="1"
                       Text="Select a Bannerlord Coop native backup from the active save's directory. The paired .sav and .json files will replace the current save while the server is fully stopped."
                       TextWrapping="Wrap"
                       Margin="0,4,0,12"/>

            <ListView x:Name="BackupListView"
                      Grid.Row="2"
                      SelectionMode="Single"
                      SelectionChanged="BackupListView_SelectionChanged">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Backup"
                                        Width="260"
                                        DisplayMemberBinding="{Binding Name}"/>

                        <GridViewColumn Header="Date modified"
                                        Width="240"
                                        DisplayMemberBinding="{Binding DateModified, StringFormat={}{0:G}}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <TextBlock x:Name="EmptyBackupsText"
                       Grid.Row="3"
                       Text="No complete native .backupN.sav/.backupN.json pairs were found for the currently configured save."
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource MutedForegroundBrush}"
                       Margin="0,8,0,0"
                       Visibility="Collapsed"/>
        </Grid>
    </DockPanel>
</Window>
