<Window x:Class="BCSTool.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About BCS Tool"
        Width="430"
        Height="335"
        MinWidth="430"
        MinHeight="335"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Bannerlord Coop Server Tool"
                   FontSize="22"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"/>

        <TextBlock x:Name="VersionText"
                   Grid.Row="1"
                   Margin="0,4,0,0"
                   HorizontalAlignment="Center"
                   Foreground="Gray"/>

        <TextBlock Grid.Row="2"
                   Margin="0,14,0,0"
                   Text="Bannerlord Coop dedicated server management tool"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="3"
                    Margin="0,14,0,0"
                    HorizontalAlignment="Center">
            <TextBlock Text="Developer: Apar"
                       HorizontalAlignment="Center"/>

            <TextBlock Margin="0,3,0,0"
                       HorizontalAlignment="Center">
                <Hyperlink NavigateUri="https://github.com/AppleDeath318"
                           RequestNavigate="AuthorProfile_RequestNavigate">github.com/AppleDeath318</Hyperlink>
            </TextBlock>
        </StackPanel>

        <TextBlock x:Name="ErrorText"
                   Grid.Row="4"
                   Margin="0,12,0,0"
                   Foreground="#B3261E"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>

        <Grid Grid.Row="5"
              Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="UpdateButton"
                    Grid.Column="0"
                    Margin="0,0,5,0"
                    Click="UpdateButton_Click"/>

            <Button Grid.Column="1"
                    Margin="5,0,0,0"
                    Content="GitHub Repository"
                    Click="GitHubRepository_Click"/>
        </Grid>
    </Grid>
</Window>
