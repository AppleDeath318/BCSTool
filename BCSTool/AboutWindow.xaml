<Window x:Class="BCSTool.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About BCS Tool"
        Width="430"
        Height="400"
        MinWidth="430"
        MinHeight="400"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppForegroundBrush}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Bannerlord Coop Server Tool"
                   FontSize="22"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"/>

        <TextBlock x:Name="VersionText"
                   Grid.Row="1"
                   Margin="0,4,0,0"
                   HorizontalAlignment="Center"
                   Foreground="{DynamicResource MutedForegroundBrush}"/>

        <StackPanel Grid.Row="2"
                    Margin="0,14,0,0"
                    HorizontalAlignment="Center">
            <TextBlock x:Name="DeveloperNameText"
                       Text="Developer: Apar"
                       HorizontalAlignment="Center"/>

            <Grid Width="80"
                  Height="80"
                  Margin="0,8,0,0"
                  HorizontalAlignment="Center">
                <Ellipse Fill="{DynamicResource ControlAltBackgroundBrush}"
                         Stroke="{DynamicResource ControlBorderBrush}"
                         StrokeThickness="1"/>

                <TextBlock x:Name="AvatarPlaceholderText"
                           Text="?"
                           FontSize="28"
                           Foreground="{DynamicResource MutedForegroundBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>

                <Ellipse x:Name="DeveloperAvatar"
                         Stroke="{DynamicResource ControlBorderBrush}"
                         StrokeThickness="1"
                         Visibility="Collapsed"
                         AutomationProperties.Name="Developer GitHub avatar"/>
            </Grid>

            <TextBlock Margin="0,6,0,0"
                       HorizontalAlignment="Center">
                <Hyperlink x:Name="AuthorProfileLink"
                           NavigateUri="https://github.com/AppleDeath318"
                           RequestNavigate="AuthorProfile_RequestNavigate">
                    <Run x:Name="AuthorProfileLinkText"
                         Text="github.com/AppleDeath318"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <Grid Grid.Row="3"
              Margin="0,12,0,0"
              VerticalAlignment="Center">
            <TextBlock x:Name="ErrorText"
                       Foreground="{DynamicResource ErrorForegroundBrush}"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="Collapsed"/>

            <StackPanel x:Name="DownloadProgressPanel"
                        Visibility="Collapsed">
                <ProgressBar x:Name="DownloadProgressBar"
                             Height="12"
                             Minimum="0"
                             Maximum="100"
                             Background="{DynamicResource ControlBackgroundBrush}"
                             Foreground="{DynamicResource AccentForegroundBrush}"
                             BorderBrush="{DynamicResource ControlBorderBrush}"/>

                <TextBlock x:Name="DownloadProgressText"
                           Margin="0,5,0,0"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource MutedForegroundBrush}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="4"
              Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="UpdateButton"
                    Grid.Column="0"
                    Margin="0,0,5,0"
                    Click="UpdateButton_Click"/>

            <Button Grid.Column="1"
                    Margin="5,0,0,0"
                    Content="GitHub Repository"
                    Click="GitHubRepository_Click"/>
        </Grid>
    </Grid>
</Window>
